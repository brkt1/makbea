import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['wmcfsndlcbmwnxyzyzrw.supabase.co'], // Allow images from Supabase
  },
  /* config options here */
};

export default nextConfig;
