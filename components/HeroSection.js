'use client'; // Marking this component as a client component

const HeroSection = () => {
    return (
        <section id="hero" className="py-16 relative overflow-hidden">
            {/* Hero content goes here */}
        </section>
    );
};

export default HeroSection;
